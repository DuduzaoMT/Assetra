# Database Configuration
DB_USER=postgres
DB_PASSWORD=your_secure_password_here
DB_NAME=assetra
DB_HOST=localhost
DB_PORT=5432

# Security
# IMPORTANT: Generate a random key with at least 32 characters for production
# You can use: openssl rand -base64 64
SECURITY_KEY=your_very_long_and_random_secret_key_minimum_32_characters

# Environment
ENV=development

# CORS (only for production)
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
